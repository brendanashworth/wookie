var assert = require('chai').assert;

describe('wookie http server', function() {
	describe('wookie http parser', function() {
		it('should give 200 on GET / request', function() {
			assert.ok(true, 'perfect');
		});
	});
});